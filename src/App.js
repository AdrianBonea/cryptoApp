import Main from "./containers/Main";

const App = () => {
  // const coinElements = coinData.map((coin) => (
  //   <AddTicker key={coin.id} {...coin} />
  // ));

  return (
    <section>
      <Main />
    </section>
  );
};

export default App;
